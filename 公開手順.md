# 公開手順 — AIxSA Compass LP

| 項目 | 値 |
|---|---|
| 公開 URL | https://compass.info-shared.com |
| Worker | `compass-lp`（Cloudflare Workers・静的アセット配信） |
| GitHub | `tk111git/compass-lp`（`main` = 保管場所。**push しても本番には出ない** → §4） |
| 本番へ出す方法 | **手元から送る**（`wrangler deploy`・§2 の ④） |
| 文章の正本 | `dist/index.html` の 1 ファイルのみ |

ビルド工程はありません。`wrangler.jsonc` の `assets.directory = ./dist` により、
`dist/index.html` がそのまま配信されます。**HTML を直接書き換えるのが唯一の編集方法**です。

---

## 1. 文章の場所（`dist/index.html`）

- **11〜870 行** = CSS。文章編集では触らない。
- **872〜1207 行** = `<body>`。文章はすべてここ。

| セクション | 行 | 見出し |
|---|---|---|
| nav | 875 | — |
| hero | 888 | `Compass` |
| about | 990 | 最初にお伝えしたい、3つのこと |
| principles | 1019 | 私たちの考え方 |
| plans | 1049 | 3つのプラン |
| subsidy | 1139 | 助成金について |
| contact | 1150 | まずは、お話だけでも。 |
| footer | 1180 | — |

※ 行番号は編集で前後します。目印は見出しテキストで検索するのが確実です。

---

## 2. 手順

以下はすべて `git -C` / 絶対パス指定なので、**どのディレクトリで打っても同じ動作**をします。

```bash
# ① 編集
open -a "Visual Studio Code" "/Users/takanori/DXサブスク/compass-lp/dist/index.html"

# ② ローカル確認（サーバ不要・ファイルを直接開けば本番と同一の見た目）
open "/Users/takanori/DXサブスク/compass-lp/dist/index.html"

# ③ 差分の確認
git -C "/Users/takanori/DXサブスク/compass-lp" diff

# ④ 公開（これを打った時だけ本番に出る。数十秒で反映）
cd "/Users/takanori/DXサブスク/compass-lp" && PATH=/usr/local/bin:$PATH npx --yes wrangler@4 deploy

# ⑤ 反映確認（本番の中身と手元のファイルが一致すれば成功）
curl -s "https://compass.info-shared.com/?cb=$RANDOM" | shasum
shasum "/Users/takanori/DXサブスク/compass-lp/dist/index.html"

# ⑥ 記録（本番には影響しない。履歴を残すためだけ）
git -C "/Users/takanori/DXサブスク/compass-lp" add -A && \
git -C "/Users/takanori/DXサブスク/compass-lp" commit -m "compass-lp: 〈変更内容〉" && \
git -C "/Users/takanori/DXサブスク/compass-lp" push origin main
```

### ④ が「ログインしていません」で止まる場合

Cloudflare の許可証は期限切れになります（自動更新される作りですが、**現に失敗していました** → §4）。
次の 1 行を打ち、開いたブラウザで **Authorize を押す**。押すまでの猶予は約 2 分です。

```bash
PATH=/usr/local/bin:$PATH npx --yes wrangler@4 login
```

- `PATH=/usr/local/bin:$PATH` は「新しい方の Node を使う」指定。既定の Node は v14 で Wrangler が動かない。
- 許可証の保存先 = `~/Library/Preferences/.wrangler/config/default.toml`（本人のみ読める権限）。
  期限は同ファイルの `expiration_time` で読める。
- 今ログインできているかの確認 = `PATH=/usr/local/bin:$PATH npx --yes wrangler@4 whoami`
- 許可の取り消し = Cloudflare の My Profile → Access Management → Connected Applications

---

## 3. 注意

- **ステージング環境はありません。** `workers_dev` と `preview_urls` は無効化済みで、
  ④ を打った瞬間に本番へ出ます。確認は必ず ② のローカルプレビューで済ませてから ④ を打つこと。
- **切り戻し**は、戻したい内容に直してから ④ を打ち直す（本番に出るのは手元のファイルの中身）:
  `git -C "/Users/takanori/DXサブスク/compass-lp" revert HEAD` → ④ → ⑤
- **見た目（色・余白）の変更**は 11〜870 行の CSS 側。cf-base の
  `docs/PJ横断情報/SUITE-DESIGN-PRINCIPLE.md` の夜空文法（compass の star は青 1 色 = L-5 等）が
  実装済みの定数として効いているため、文章編集とは切り分けて扱う。
- **DNS / Custom Domain の結線は完了済み**。触る必要はない（手動 CNAME 追加は禁止）。
- cf-base 側の記帳（`docs/cloudflare_operational_status.md` §7 / `src/slots.json`）は
  スロットの状態を記録したもので、文言変更では更新不要。

---

## 4. 2026-09-18 に分かったこと（本書の前提が変わった経緯）

**GitHub へ push しても本番には出ません。** 本書は 2026-09-02 まで「push が本番デプロイのトリガー」と
書いていましたが、その仕組みは働いていませんでした。

### 実測（2026-09-18）

| 測ったもの | 結果 |
|---|---|
| 本番ページの中身 | 2026-07-04 の commit `a2808a6` と byte 一致（9/2 の commit `eb29d05` ではない） |
| Worker `compass-lp` の公開履歴 | 全 4 件。7/4 に 3 件（Upload / Unknown）＋ 9/18 に 1 件（手元から送った分）。**9/2 の記録は無い** |
| GitHub 側のビルド記録 | 9/2 以降 0 件（check-runs / deployments とも） |

∴ **9/2 の文章（自社開発 AI を前面に・開発と保守の線引き・ヒアリング約 60 分 等 10 か所）は、
16 日間そのまま埋もれていた。** 9/18 に手元から送って、利用例 3 件と一緒に本番へ出した
（commit `12162a5` / Version ID `e694ca55-64e7-4a39-9062-18f07b46c86f`）。

### 止まっている理由 — **GitHub との接続が外れていた**（2026-09-18 に画面で確認）

Cloudflare の画面（Worker `compass-lp` → Settings → Builds）の冒頭に、こう出ていました。

> This project is disconnected from your Git account. This may cause deployments to fail.

- `Git repository` 欄には `tk111git/compass-lp` が**残っています**。∴ ぱっと見は「つながっている」
  ように見え、外れたことは冒頭のこの 1 文でしか分かりません。
- Build command / Deploy command / Root directory は空。Deploy Hooks も無し。
- 同じ画面の Trigger events は「静的アセットだけの Worker にトリガーは追加できない」と表示。

**∴ 失敗が見えない仕組み**: push は成功し（GitHub は受け取る）、ビルドは起動せず、
GitHub 側にも Cloudflare 側にも記録が残りません。落ちるのではなく、始まらないのです。

なお、この接続状態は機械では読めません（Workers Builds の API は wrangler の許可証の
権限の外 = 403 Authentication error）∴ 判定は §2 の ⑤（本番と手元の sha 照合）で行う。

### 再接続はしない

静的アセットだけのこの星は、そもそもこの仕組みと相性がよくありません（上の Trigger events）。
一度黙って外れた経路に戻すより、**出すたびに ⑤ で本番と手元を照合する**今の形を正とします
（2026-09-18・L0 承認）。再接続したくなった場合の手順は cf-base の
`cloudflare_howto_playbook.md` §3 B-3 にあります。

### 許可証（Cloudflare へのログイン）も切れていた

`~/Library/Preferences/.wrangler/config/default.toml` の `expiration_time` は **2026-07-27** のまま
でした。更新用の鍵（`offline_access`）を持つ作りなのに自動更新されておらず、9/18 に打った
`whoami` は「期限切れで更新もできない」で止まりました。→ §2 の ④ が止まったら再ログインする。

### この形にした理由

自動で出る仕組みは、黙って止まっても誰も気づけません（現に 16 日気づけなかった）。
手元から送る形なら、出したその場で ⑤ の 2 行で本番と手元の一致を確かめられます。
